import React from 'react';
import { render } from '@testing-library/react';

const Header = () => {
  return (
    <header>
      <h2>Companies header and logo </h2>
    </header>
  )
};

export default Header;