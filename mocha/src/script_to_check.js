// 1.
function pow(i, x) {
    return Math.pow(i, x);
};

// 2.
function add(i, x){
    return i + x
}

// 3.
function checkNumber(n) {
    var isNo = Number.isInteger(n) ? 1 : false;
    return isNo;
};

// 4.
var arrayToCheck = [1, 2, 3, 4, 5, 6, 7];

// 5.
class MyClass {
    constructor() {
        name: "Rafal"
    };
};

var mc = new MyClass();

