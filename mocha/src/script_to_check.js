function pow(i, x) {
    return Math.pow(i, x);
};

function checkNumber(n) {
    var isNo = Number.isInteger(n) ? 1 : false;
    return isNo;
};

var arrayToCheck = [1, 2, 3, 4, 5, 6, 7];

class MyClass {
    constructor() {
        name: "Rafal"
    };
};

var mc = new MyClass();

