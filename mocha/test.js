describe("pow", () => {
  it("raises to n-th power", () => {
    assert.equal(pow(2, 3), 8);
    assert.equal(pow(3, 4), 81);
  });
 });

describe("sum", function () {
  it("adds 2 numners", function () {
    expect(add(2, 2)).equal(4);
    expect(add(5, 5)).equal(10);
  });
});
